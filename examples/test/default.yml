name: Default
vars:
  URL: "http://127.0.0.1:8080"

testcases:

################################ GET NO_MODEL ##################################

- name: no_model
  steps:
  - type: tavern
    request:
      url: "{{.URL}}/noModel"
      method: GET
      headers:
        authorization: foo
        token: bar
    response:
      statusCode: 200
      headers:
        Content-Type: "application/json; charset=utf-8"
      json:
        authorization: foo
        token: bar

- name: no_model_default
  steps:
  - type: tavern
    request:
      url: "{{.URL}}/noModel"
      method: GET
    response:
      statusCode: 200
      headers:
        Content-Type: "application/json; charset=utf-8"
      json:
        authorization: authorization
        token: token
